import createStudent, { CREATE_STUDENT } from './create'
import fetchOneStudent, { FETCHED_ONE_STUDENT } from './fetch'
import rateStudent, { RATE_STUDENT } from './rate'
import updateStudent, { UPDATE_STUDENT } from './update'
import deleteStudent, { DELETE_STUDENT } from './delete'

export {
  createStudent,
  rateStudent,
  fetchOneStudent,
  updateStudent,
  deleteStudent,
  CREATE_STUDENT,
  FETCHED_ONE_STUDENT,
  RATE_STUDENT,
  UPDATE_STUDENT,
  DELETE_STUDENT
}
